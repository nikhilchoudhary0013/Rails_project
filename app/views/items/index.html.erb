<div class="item1">
   <h1 id="h1">Listing Items</h1>
    <% @items.each do |item| %>
      <div>
        <a href="/items/ <%= item.id %>">
        <%= item.name %>
      </div>
    <% end %>
</div>

<%= link_to "New item", new_item_path %>