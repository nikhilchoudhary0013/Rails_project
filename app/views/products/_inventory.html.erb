<% if product.inventory_count.to_i.positive? %>
  <p><%= product.inventory_count %> in stock</p>
<% else %>
  <p>Out of stock</p>
  <p>Email me when available.</p>

  <%= form_with model: [product, Subscriber.new] do |form| %>
    <%= form.email_field :email, placeholder: "example@123.com", required: true %>
    <%= form.submit "Submit" %>
  <% end %>
<% end %>
